document.addEventListener('DOMContentLoaded', function(){
describe("Note class", () => {
  
  it("some and some", () => {
    
    expect(1).toEqual(1);
  })

  it("some and some", () => {
    
    expect(1).toEqual(2);
  })



});
})