document.addEventListener('DOMContentLoaded', function(){
describe("Note class", () => {
  
  it("list of all the notes", () => {
    let note = new Note
    message = note.createNote("Birthday list:")
    expect("").toEqual("Birthday list:")
  })

  it("some and some new", () => {
    
    expect(1).toEqual(1);
  })



});
})